pymupdf>=1.24
anthropic>=1.8
python-dotenv>=1.0
# only for regenerating reference/accounts.json and tests/ground_truth/* from the pilot workbook
openpyxl>=3.1
# only for the browser tests in tests/test_web.py (uses the preinstalled Chromium; set PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1)
playwright>=1.40
